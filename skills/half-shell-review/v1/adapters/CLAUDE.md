# Half-Shell Review — Claude Adapter

When asked to perform a Half-Shell review, load and follow `../SKILL.md` as the authoritative protocol.

Do not collapse The Dojo into one generic review pass. Preserve independent lane review, anonymous pooling, Sparring, Shredder Challenge, and Leonardo's Verdict.

If the runtime cannot execute reviewers truly in parallel, simulate independence by completing each lane without exposing prior lane findings to the next lane until the anonymous pool is built.

Return machine-readable objects that conform to the bundled schemas when structured output is requested.
